<UserControl x:Class="RunescapeCacheToolsGUI.ExtractJobUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
			 d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ProgressBar x:Name="progressBar" Margin="10,10,10,5" Height="20"/>
        <Label x:Name="statusLabel" Content="Label" HorizontalAlignment="Left" Margin="10,5,0,5" Grid.Row="1" Padding="1"/>
        <Button x:Name="actionButton" Content="Close" Margin="0,5,10,5" Grid.Row="1" HorizontalAlignment="Right" Click="actionButton_Click"/>
    </Grid>
</UserControl>
