<UserControl x:Class="RuneScapeCacheToolsGUI.CacheJobUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
			 d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label x:Name="statusLabel" HorizontalAlignment="Left" Margin="10,5,0,2.5" Grid.Row="1" Padding="1"/>
        <Button x:Name="cancelButton" Content="Cancel" Margin="0,2.5,10,5" Grid.Row="1" HorizontalAlignment="Right" Click="cancelButton_Click"/>
        <ProgressBar x:Name="progressBar" Margin="10,2.5,10,2.5" Height="20" Grid.Row="0"/>
        <TextBlock x:Name="percentageTextBlock" Margin="0" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="0"
			Text="{Binding ElementName=progressBar, Path=Value, StringFormat={}{0:0}%}"/>
    </Grid>
</UserControl>
